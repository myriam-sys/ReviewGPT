# ── Web framework ──────────────────────────────────────────────────────────────
fastapi==0.115.0              # Async web framework for building the REST API
uvicorn[standard]==0.30.6     # ASGI server to run FastAPI (includes websockets + reload)

# ── Configuration ──────────────────────────────────────────────────────────────
pydantic-settings==2.4.0      # Load and validate environment variables with Pydantic models
python-dotenv==1.0.1          # Parse .env files into os.environ

# ── Data handling ──────────────────────────────────────────────────────────────
pandas==2.2.2                 # CSV parsing and tabular data manipulation
python-multipart==0.0.9       # Required by FastAPI to handle file uploads (multipart/form-data)

# ── Embeddings ─────────────────────────────────────────────────────────────────
sentence-transformers==3.1.1  # Load and run multilingual-e5-large for text embedding
torch==2.4.1                  # PyTorch backend required by sentence-transformers
transformers==4.44.2          # HuggingFace model hub & tokenizer utilities

# ── Vector database ────────────────────────────────────────────────────────────
supabase==2.7.4               # Official Supabase Python client (REST + realtime)
vecs==0.4.3                   # Supabase vector store helper for pgvector operations

# ── LLM ────────────────────────────────────────────────────────────────────────
groq==0.11.0                  # Official Groq Python SDK for calling llama3/mixtral

# ── HTTP client ────────────────────────────────────────────────────────────────
httpx==0.27.2                 # Async HTTP client used internally by Supabase SDK

# ── Excel support ──────────────────────────────────────────────────────────────
openpyxl==3.1.5               # Read/write XLSX files via pd.read_excel(engine='openpyxl')

# ── Language detection & date parsing ─────────────────────────────────────────
langdetect==1.0.9             # Detect ISO 639-1 language code from review text
dateparser==1.2.0             # Parse relative/multilingual date strings ("il y a 2 ans", "3 months ago")

# ── Utilities ──────────────────────────────────────────────────────────────────
numpy==1.26.4                 # Numerical arrays, required for embedding vectors
tenacity==8.5.0               # Retry logic for flaky external API calls
